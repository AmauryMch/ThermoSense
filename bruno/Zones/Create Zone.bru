meta {
  name: Create Zone
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/buildings/{{buildingId}}/zones
  body: json
  auth: bearer
}

body:json {
  {
    "name": "Zone D — Cafétéria"
  }
}


auth:bearer {
  token: {{token}}
}