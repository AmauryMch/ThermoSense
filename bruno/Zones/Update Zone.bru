meta {
  name: Update Zone
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/buildings/{{buildingId}}/zones/{{zoneId}}
  body: json
  auth: bearer
}

body:json {
  {
    "name": "Zone A — Open Space (réaménagé)"
  }
}


auth:bearer {
  token: {{token}}
}