meta {
  name: Update Building
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/buildings/{{buildingId}}
  body: json
  auth: bearer
}

body:json {
  {
    "name": "Siège Social ThermoSense (rénové)"
  }
}


auth:bearer {
  token: {{token}}
}