meta {
  name: Create Sensor
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/buildings/{{buildingId}}/zones/{{zoneId}}/sensors
  body: json
  auth: bearer
}

body:json {
  {
    "type": "both",
    "label": "Capteur mixte test"
  }
}


auth:bearer {
  token: {{token}}
}