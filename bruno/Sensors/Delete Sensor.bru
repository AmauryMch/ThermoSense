meta {
  name: Delete Sensor
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/buildings/{{buildingId}}/zones/{{zoneId}}/sensors/{{sensorId}}
  body: none
  auth: bearer
}


auth:bearer {
  token: {{token}}
}