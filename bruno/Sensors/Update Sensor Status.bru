meta {
  name: Update Sensor Status
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/buildings/{{buildingId}}/zones/{{zoneId}}/sensors/{{sensorId}}
  body: json
  auth: bearer
}

body:json {
  {
    "status": "maintenance"
  }
}


auth:bearer {
  token: {{token}}
}