meta {
  name: Create User
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "nouvel_operateur",
    "password": "Operateur1234!",
    "role": "operator",
    "zoneId": "z-001"
  }
}
