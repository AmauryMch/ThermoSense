meta {
  name: Login (device zone A)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "device_zone_a",
    "password": "Device1234!"
  }
}

script:post-response {
  if (res.status === 200 && res.body.token) {
    bru.setVar("token", res.body.token);
    console.log("Token device_zone_a sauvegardé (zoneId=z-001)");
  }
}
